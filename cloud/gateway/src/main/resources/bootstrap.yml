# Tomcat
server:
  version: 1.0.0
  port: 18080

# Spring
spring:
#  LOWER_CAMEL_CASE    userName    默认策略
#  KEBAB_CASE    user-name
#  LOWER_CASE    username
#  SNAKE_CASE    user_name
#  UPPER_CAMEL_CASE    UserName
  jackson:
    property-naming-strategy: LOWER_CAMEL_CASE
  application:
    # 应用名称
    name: demo-gateway
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 127.0.0.1:8848
        # 用户名
        username: nacos
        # 密码
        password: nacos
        # 命名空间
        namespace: demo
#        group: ${nacos.group} # 使用的 Nacos 配置分组，默认为 DEFAULT_GROUP
      config:
        bootstrap:
          enable: true
        # 配置中心地址
        server-addr: 127.0.0.1:8848
        # 用户名
        username: nacos
        # 密码
        password: nacos
        # 命名空间
        namespace: demo
        # 配置文件格式
        file-extension: yaml
        # 共享配置
        shared-configs:
          - data-id: ${spring.application.name}-${spring.profiles.active}.yaml
#            group: demo
            refresh: true
          - data-id: demo-common.yaml
#            group: demo
            refresh: true
          - data-id: demo-common-${spring.profiles.active}.yaml
#            group: demo
            refresh: true
#spring:
  datasource:
    redis:
      host: 127.0.0.1 # 地址
      port: 6379 # 端口
      database: 11 # 数据库索引
      password: '000000'
      pool:
        maxTotal: 1000
        maxIdle: 10000
#    sentinel:
#      # 取消控制台懒加载
#      eager: true
#      transport:
#        # 控制台地址
#        dashboard: 127.0.0.1:8718
#      # nacos配置持久化
#      datasource:
#        ds1:
#          nacos:
#            server-addr: 127.0.0.1:8848
#            dataId: demo-gateway
#            groupId: DEFAULT_GROUP
#            data-type: json
#            rule-type: gw-flow
